import {useEffect,useState} from "react";
import {NavLink,Route,Routes,useNavigate,useParams} from "react-router-dom";
import "./styles.css";

type Org={id:string;name:string;license_status:string;license_started_at?:string;license_expires_at?:string;title?:string;tagline?:string;subscript?:string;logo_url?:string;is_active:boolean};
const api=async(path:string,opts:any={})=>{const token=localStorage.getItem("token");const r=await fetch(path,{...opts,headers:{"Content-Type":"application/json",...(token?{Authorization:`Bearer ${token}`}:{ }),...(opts.headers||{})}});if(!r.ok)throw new Error((await r.json().catch(()=>({detail:"Request failed"}))).detail||"Request failed");return r.status===204?null:r.json()};
function Page({title,children,action}:{title:string;children:any;action?:any}){return <main><header className="topbar"><div><span className="eyebrow">ALI SIMULATIONS</span><h1>{title}</h1></div>{action||<div className="avatar">PA</div>}</header>{children}</main>}
function Dashboard(){const[d,setD]=useState<any>({});useEffect(()=>{api('/api/platform/dashboard').then(setD).catch(()=>{})},[]);return <Page title="Platform Overview"><section className="hero"><div><span className="eyebrow">LEADERSHIP, EXPERIENCED.</span><h2>Build experiences that reveal how people lead.</h2><p>Manage organizations, launch simulations, observe leadership behavior, and turn experience into meaningful feedback.</p></div></section><section className="metrics">{[["Organizations",d.organizations??"—"],["People",d.people??"—"],["Sessions",d.sessions??"—"],["Simulation Types",d.simulations??"—"]].map(x=><article className="metric" key={x[0]}><span>{x[0]}</span><strong>{x[1]}</strong><small>Persistent platform data</small></article>)}</section><section className="grid"><article className="panel"><h2>Start building the platform</h2><p>Create Early Learning Collective as the first organization, then configure its branding and simulation access.</p><NavLink className="button" to="/organizations/new">Create organization</NavLink></article><article className="panel"><h2>Data Protection</h2><div className="check">✓ Existing records preserved across deploys</div><div className="check">✓ Forward-only schema migrations</div><div className="check">✓ Administrative changes audited</div></article></section></Page>}
function Organizations(){const[rows,setRows]=useState<Org[]>([]);useEffect(()=>{api('/api/platform/organizations').then(setRows)},[]);return <Page title="Organizations" action={<NavLink className="button" to="/organizations/new">+ Add Organization</NavLink>}><section className="panel"><div className="panelhead"><div><h2>Organization Directory</h2><p className="muted">Licensed organizations and their platform status.</p></div></div>{rows.length?<div className="table">{rows.map(o=><NavLink to={`/organizations/${o.id}`} className="row" key={o.id}><div><b>{o.name}</b><small>{o.tagline||"No tagline configured"}</small></div><span className={`pill ${o.is_active?'good':''}`}>{o.license_status}</span><span>View →</span></NavLink>)}</div>:<div className="empty"><strong>No organizations yet</strong><span>Create Early Learning Collective as your first organization.</span></div>}</section></Page>}
function OrgForm(){const{id}=useParams();const nav=useNavigate();const[form,setForm]=useState<any>({name:"",license_status:"active",title:"",tagline:"",subscript:"",logo_url:"",is_active:true});const[err,setErr]=useState("");useEffect(()=>{if(id)api(`/api/platform/organizations/${id}`).then(setForm)},[id]);const save=async(e:any)=>{e.preventDefault();setErr("");try{const saved=await api(id?`/api/platform/organizations/${id}`:'/api/platform/organizations',{method:id?'PUT':'POST',body:JSON.stringify(form)});nav(`/organizations/${saved.id}`)}catch(e:any){setErr(e.message)}};return <Page title={id?"Organization Settings":"Add Organization"}><form className="panel form" onSubmit={save}><h2>{id?"Organization Profile":"Create an organization"}</h2>{err&&<div className="error">{err}</div>}<label>Organization name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Early Learning Collective"/></label><div className="two"><label>License status<select value={form.license_status} onChange={e=>setForm({...form,license_status:e.target.value})}><option value="active">Active</option><option value="trial">Trial</option><option value="suspended">Suspended</option><option value="expired">Expired</option></select></label><label className="toggle"><input type="checkbox" checked={form.is_active} onChange={e=>setForm({...form,is_active:e.target.checked})}/> Organization active</label></div><h3>Branding</h3><label>Display title<input value={form.title||""} onChange={e=>setForm({...form,title:e.target.value})}/></label><label>Tagline<input value={form.tagline||""} onChange={e=>setForm({...form,tagline:e.target.value})}/></label><label>Subscript<textarea value={form.subscript||""} onChange={e=>setForm({...form,subscript:e.target.value})}/></label><label>Logo URL<input value={form.logo_url||""} onChange={e=>setForm({...form,logo_url:e.target.value})}/></label><div className="actions"><button type="button" className="secondary" onClick={()=>nav('/organizations')}>Cancel</button><button>Save Organization</button></div></form></Page>}
function OrgDetail(){const{id}=useParams();const[o,setO]=useState<Org|null>(null);const[ents,setEnts]=useState<any[]>([]);useEffect(()=>{api(`/api/platform/organizations/${id}`).then(setO);api(`/api/platform/organizations/${id}/entitlements`).then(setEnts)},[id]);const toggle=async(x:any)=>{await api(`/api/platform/organizations/${id}/entitlements/${x.simulation_type_id}`,{method:'PUT',body:JSON.stringify({enabled:!x.enabled})});setEnts(ents.map(e=>e.simulation_type_id===x.simulation_type_id?{...e,enabled:!e.enabled}:e))};if(!o)return <Page title="Organization"><div className="panel">Loading…</div></Page>;return <Page title={o.name} action={<NavLink className="button" to={`/organizations/${id}/edit`}>Edit Organization</NavLink>}><section className="grid"><article className="panel"><h2>Overview</h2><dl><dt>Status</dt><dd>{o.license_status}</dd><dt>Access</dt><dd>{o.is_active?'Active':'Disabled'}</dd><dt>Title</dt><dd>{o.title||'—'}</dd><dt>Tagline</dt><dd>{o.tagline||'—'}</dd><dt>License expiration</dt><dd>{o.license_expires_at||'No expiration'}</dd></dl></article><article className="panel"><h2>Simulations & Licensing</h2>{ents.map(x=><div className="ent" key={x.simulation_type_id}><div><b>{x.name}</b><small>{x.lifecycle}</small></div><button className={x.enabled?'enabled secondary':''} onClick={()=>toggle(x)}>{x.enabled?'Enabled':'Enable'}</button></div>)}{!ents.length&&<p className="muted">Simulation catalog will appear after startup seeding.</p>}</article></section></Page>}
function Audit(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{api('/api/platform/audit').then(setRows)},[]);return <Page title="Audit History"><section className="panel"><h2>Recent administrative activity</h2>{rows.map(x=><div className="audit" key={x.id}><div><b>{x.action}</b><small>{x.entity_type} · {x.entity_id||'—'}</small></div><time>{new Date(x.created_at).toLocaleString()}</time></div>)}{!rows.length&&<div className="empty">No audit events yet.</div>}</section></Page>}
function Placeholder({title,text}:{title:string;text:string}){return <Page title={title}><section className="panel"><h2>{title}</h2><p>{text}</p><div className="empty"><strong>Next build area</strong><span>The navigation is ready; functionality will be added without replacing your existing data.</span></div></section></Page>}
const nav=[["/","Home"],["/organizations","Organizations"],["/people","People"],["/simulations","Programs & Simulations"],["/sessions","Sessions"],["/leadership","Leadership Development"],["/communications","Communications"],["/reports","Reports"],["/admin","Administration"]];
export default function App(){return <div className="app"><aside><div className="brand"><div className="mark">▲</div><div><b>ALI</b><span>SIMULATIONS</span><small>Leadership, Experienced.</small></div></div><nav>{nav.map(([u,n])=><NavLink key={u} to={u} end={u==="/"}>{n}</NavLink>)}</nav><div className="user"><b>Platform Administrator</b><span>Persistent development environment</span></div></aside><Routes><Route path="/" element={<Dashboard/>}/><Route path="/organizations" element={<Organizations/>}/><Route path="/organizations/new" element={<OrgForm/>}/><Route path="/organizations/:id" element={<OrgDetail/>}/><Route path="/organizations/:id/edit" element={<OrgForm/>}/><Route path="/people" element={<Placeholder title="People" text="Canonical directory and organization memberships are next."/>}/><Route path="/simulations" element={<Placeholder title="Programs & Simulations" text="Belonging, CityLab, scenario lifecycle, and catalog management."/>}/><Route path="/sessions" element={<Placeholder title="Sessions" text="Create and operate simulation sessions."/>}/><Route path="/leadership" element={<Placeholder title="Leadership Development" text="Observations, evaluations, approval, release, and leadership journeys."/>}/><Route path="/communications" element={<Placeholder title="Communications" text="Relationship-based audiences, templates, and delivery history."/>}/><Route path="/reports" element={<Placeholder title="Reports" text="Curated participant, session, program, and organization outcomes."/>}/><Route path="/admin" element={<Audit/>}/></Routes></div>}
