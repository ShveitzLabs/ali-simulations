# ALI Simulations v0.3.0 — Testable Interface Shell

Adds a deployable React application shell and the first testable interface pages:
Home, Organizations, People, Programs & Simulations, Sessions, Leadership Development,
Communications, Reports, and Administration.

Also restores the missing global stylesheet and adds React Router.
The current pages are intentionally interface-first; data/API wiring follows in the next build.
